[variables]
NIXPACKS_PHP_EXTENSIONS = 'pdo,pdo_mysql,intl,ctype,iconv,mbstring,zip'

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader']

[phases.build]
cmds = ['php bin/console cache:clear']

[start]
cmd = 'php -S 0.0.0.0:$PORT -t public'
